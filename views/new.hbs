{{> navbar}}
{{> message}}
<form action="/todos" method="POST">
  <input type="text" placeholder="name" name="name"> 
  {{!-- name屬性是HTTP傳送資料時的名稱，會被放在HTTP的request body裡 --}}
  <button type="submit">Create</button>
</form>